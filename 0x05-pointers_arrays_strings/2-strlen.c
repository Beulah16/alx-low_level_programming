#include <stdio.h>
#include "main.h"

/**
 * _strlen -A function
 * @s: a string to be evaluated
 *
 * Return: Length of string
 */
int _strlen(char *s)
{
	int length;

	length = strlen(s);

	return (length);
}
